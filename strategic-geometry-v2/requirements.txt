# Analysis (CPU) — needed for dataset migration + Stage-A geometry/nulls/calibration
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
pyyaml>=6.0

# Generation / extraction (GPU host only; not needed for Stage-A analysis)
# torch>=2.2
# transformers>=4.44
# accelerate
# tqdm

# Annotation (LLM judge) — host with API access
# openai>=1.0
# pydantic>=2.0

# Dev
pytest>=7.0
